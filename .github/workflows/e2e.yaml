---
# yaml-language-server: $schema=https://json.schemastore.org/github-workflow.json
name: E2E

on:
  pull_request:
    types:
      - opened
      - synchronize
      - reopened
    branches:
      - main
    paths:
      - .mise/**
      - charts/**
      - test/**
      - Dockerfile
      - go.mod
      - go.sum
      - "**.go"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  e2e-test:
    name: E2E (${{ matrix.controllers }}cp/${{ matrix.workers }}w)
    runs-on: ubuntu-24.04
    permissions:
      contents: read
    timeout-minutes: 45
    strategy:
      # The legs are independent local clusters, so a failure in one should not
      # cost us the signal from the other.
      fail-fast: false
      matrix:
        # Each entry runs on its own runner, so legs are cheap to add. What is
        # bounded is the VM count within one leg, at 2GiB per VM on a 16GB
        # runner.
        include:
          # Covers the self-hosted path: with one node tuppr upgrades the node
          # it is running on, so it skips the drain and issues the upgrade
          # without --wait. No other leg reaches that code.
          - controllers: 1
            workers: 0
          - controllers: 1
            workers: 1
          - controllers: 3
            workers: 0
    env:
      CONTROL_PLANE_COUNT: ${{ matrix.controllers }}
      WORKER_COUNT: ${{ matrix.workers }}
      # Fixed up front so the image build and the test run agree on the tag
      # without having to pass a value between steps.
      CONTROLLER_IMAGE: ttl.sh/tuppr-e2e-${{ github.run_id }}-${{ matrix.controllers }}cp${{ matrix.workers }}w:2h
    defaults:
      run:
        working-directory: test/e2e-qemu
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false

      - name: Setup Mise
        uses: jdx/mise-action@dad1bfd3df957f44999b559dd69dc1671cb4e9ea # v4.2.1
        with:
          experimental: true
          install_args: --locked

      - name: Install QEMU
        run: |-
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends qemu-system-x86 qemu-utils ovmf

      # Headroom for the upgrade phase, when each node unpacks the installer
      # alongside etcd and the API server. Its own file, so it adds to the
      # runner's existing swap instead of replacing it.
      - name: Enable swap
        run: |-
          sudo fallocate -l 8G /mnt/e2e-swapfile
          sudo chmod 600 /mnt/e2e-swapfile
          sudo mkswap /mnt/e2e-swapfile
          sudo swapon /mnt/e2e-swapfile

      - name: Resolve toolchain versions
        id: tools
        working-directory: .
        run: echo "go=$(mise config get tools.go)" >> "$GITHUB_OUTPUT"

      # Booting the VMs and building the controller share no inputs, so they
      # overlap. They do contend for the runner's 4 vCPUs, so the saving is
      # less than the build time, but the build leaves the critical path.
      - name: Create cluster
        id: cluster
        background: true
        run: mise exec --locked --no-deps -- ./cluster.sh

      - name: Set up Buildx
        uses: docker/setup-buildx-action@bb05f3f5519dd87d3ba754cc423b652a5edd6d2c # v4.2.0

      # All three legs build byte-identical images, so they share one cache
      # scope. Only one leg writes it back: concurrent cache-to writes to the
      # same scope evict each other.
      - name: Build controller image
        id: image
        background: true
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7.3.0
        with:
          context: .
          push: true
          tags: ${{ env.CONTROLLER_IMAGE }}
          build-args: GO_VERSION=${{ steps.tools.outputs.go }}
          cache-from: type=gha,scope=e2e-controller
          cache-to: ${{ matrix.controllers == 3 && 'type=gha,mode=max,scope=e2e-controller' || '' }}

      - wait: [cluster, image]

      - name: Run e2e tests
        run: mise exec --locked --no-deps -- ./test.sh
